<%# "Adding a new entry" view when user wants to sell a plant.  %>

<div class="col-md-6 offset-md-3">
    <div class="bg-light p-5 rounded-lg m-3">
        <div class="mb-3">

            <h3>
                <center>Details of your plant<center>
            </h3>
            <br>
            <label for="plant_name">Name of flora: <%= @plant.name %></label>
            <br>
            <label for="plant_name">Age of flora: <%= @plant.age %></label>


            <% if @plant.image.attached?%>
            <center>
                <%= image_tag url_for(@plant.image), class: "plant_view" %>
                <center>
                    <%end%>


                    <%if current_user.id == @plant.user_id %>
                    <br>
                    <div class="button-container">
                        <%= button_to "Edit", edit_plant_path(@plant), class: "btn btn-success", method: :get %>
                        <%= button_to "Destroy", plant_path(@plant), class: "btn btn-danger", 
                  method: :delete,
                  data: { confirm: "Are you sure?" } %>
                    </div>
                    <%else%>

                    <%end%>

        </div>
    </div>
</div>
<center><%= link_to "Back", plants_path, class: "btn btn-outline-secondary" %><center>